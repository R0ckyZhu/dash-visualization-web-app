[project]
name = "dash-visualizer-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.0",
    "openai>=3.8,<4",
]

[project.scripts]
dash-viz = "app.main:run"
